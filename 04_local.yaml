- name: install in local
  hosts: local
  connection: local
  become: yes
  tasks:
    - name: local
      vars:
       course: "DevOps"
      ansible.builtin.debug:
       msg: "i m traning at course {{ course }}"

    